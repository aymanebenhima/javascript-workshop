// substr(): It takes two arguments, the starting index and number of characters to slice.

let string = 'JavaScript'
console.log(string.substr(2,2))    // Script

let country = 'Morocco'
console.log(country.substr(3, 4))   // occo