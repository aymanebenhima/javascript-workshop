/**
 * Add an element from the beginning
 * 
 * unshift: Adding array element in the beginning of the arra
 */

 const numbers = [1, 2, 3, 4, 5]
 numbers.unshift(0) // -> add one item from the beginning
 
 console.log(numbers) // -> [0,1,2,3,4,5]